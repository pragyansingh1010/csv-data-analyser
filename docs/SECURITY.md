# Security Notes

The analyzer is intended for local CSV files and does not require application credentials.

Do not commit private datasets, passwords, tokens, API keys, or other sensitive information. The repository ignores CSV files by default to reduce the chance of accidentally publishing local datasets.
