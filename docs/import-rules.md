# CSV Import Rules

Validate that uploaded data has readable headers and rows before analysis. Empty files and malformed rows should produce actionable errors rather than partial, misleading statistics.