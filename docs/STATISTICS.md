# Statistics Reference

For numeric columns, the analyzer reports:

- **Mean** — arithmetic average.
- **Median** — middle value after ordering the observations.
- **Standard deviation** — spread of numeric observations.
- **Minimum** — smallest observed value.
- **Maximum** — largest observed value.

Displayed mean, median, and standard deviation values are rounded to two decimal places.