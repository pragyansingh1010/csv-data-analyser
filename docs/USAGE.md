# Usage Guide

1. Install dependencies with `pip install -r requirements.txt`.
2. Run `python "csv data analyser.py"`.
3. Enter a local CSV file path.
4. Choose an analysis option from the menu.

The analyzer keeps the source dataset in memory and does not modify the CSV file.