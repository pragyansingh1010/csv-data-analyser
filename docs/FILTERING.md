# Filtering Behavior

Filtering asks for a column name and a search value.

The selected column is converted to text before matching, allowing the same menu option to work with text and numeric-looking values. Matching is case-insensitive and supports partial text matches. Missing values are excluded from matches.
