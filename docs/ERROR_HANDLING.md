# Error Handling

The current application handles an unreadable or invalid CSV by showing a clear load error and stopping execution.

During filtering, an unknown column is rejected and the user is returned to the menu.

Invalid menu selections do not terminate the program; the menu is shown again so the user can retry.