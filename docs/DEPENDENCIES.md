# Dependencies

## Pandas
Used to load CSV files, inspect tabular data, select numeric columns, calculate statistics, detect missing values, and filter rows.

## NumPy
Used to identify numeric data types through Pandas' NumPy-backed numeric type selection.

Install both with:

```bash
pip install -r requirements.txt
```
